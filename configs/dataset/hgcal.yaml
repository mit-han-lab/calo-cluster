# @package _group_
target: datasets.hgcal.HGCalDataModule
num_classes: 4
params:
  label_type: class
  data_dir: /global/cscratch1/sd/schuya/hgcal-dev/data/hgcal
  num_events: 12
  num_train_events: 10
  num_test_events: 1
  force_download: False
  num_workers: 8
  voxel_size: 1.0
  num_epochs: ${train.num_epochs}
  batch_size: ${train.batch_size}